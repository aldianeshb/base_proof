# 🚀 Деплой BaseProof Frontend на Vercel

Пошаговая инструкция по деплою фронтенда BaseProof на Vercel.

## 📋 Предварительные требования

1. ✅ Проект запушен в GitHub
2. ✅ Контракты задеплоены на Base (Sepolia или Mainnet)
3. ✅ Backend API работает (Railway, Render, или другой хостинг)
4. ✅ WalletConnect Project ID (получить на https://cloud.walletconnect.com)

## 🎯 Способ 1: Деплой через Vercel Dashboard (Рекомендуется)

### Шаг 1: Подключение репозитория

1. Зайдите на https://vercel.com
2. Войдите в аккаунт (или создайте новый)
3. Нажмите **"Add New..."** → **"Project"**
4. Выберите **"Import Git Repository"**
5. Найдите репозиторий `aldianeshb/baseproof`
6. Нажмите **"Import"**

### Шаг 2: Настройка проекта

Vercel автоматически определит Next.js проект. Настройте:

**Root Directory:**
- Выберите `frontend` (или оставьте автоматическое определение)

**Framework Preset:**
- Должен быть `Next.js`

**Build Command:**
- Оставьте по умолчанию: `npm run build` (или `cd frontend && npm run build`)

**Output Directory:**
- Оставьте по умолчанию: `.next`

**Install Command:**
- Оставьте по умолчанию: `npm install` (или `cd frontend && npm install`)

### Шаг 3: Добавление переменных окружения

**ВАЖНО:** Добавьте все переменные **ДО** первого деплоя!

1. В разделе **"Environment Variables"** добавьте:

```
NEXT_PUBLIC_API_URL = https://your-api-url.com
NEXT_PUBLIC_PROOF_REGISTRY_ADDRESS = 0x...
NEXT_PUBLIC_CHAIN_ID = 84532
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID = your_walletconnect_project_id
```

2. Выберите окружения для каждой переменной:
   - ✅ Production
   - ✅ Preview
   - ✅ Development

3. Нажмите **"Save"**

### Шаг 4: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения деплоя (~2-3 минуты)
3. После успешного деплоя вы получите URL вида: `https://baseproof-xxx.vercel.app`

## 🎯 Способ 2: Деплой через Vercel CLI

### Шаг 1: Установка Vercel CLI

```bash
npm i -g vercel
```

### Шаг 2: Логин

```bash
vercel login
```

### Шаг 3: Деплой

```bash
cd /Users/nikolajburlakov/Work/Base/base_proof/frontend

# Первый деплой (интерактивный)
vercel

# Последующие деплои в production
vercel --prod
```

### Шаг 4: Добавление переменных окружения

```bash
# Добавить переменную
vercel env add NEXT_PUBLIC_API_URL production

# Или добавить все сразу через файл
vercel env pull .env.local
# Отредактируйте .env.local
vercel env push .env.local
```

## 🔧 Настройка переменных окружения

### Обязательные переменные:

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `NEXT_PUBLIC_API_URL` | URL вашего backend API | `https://baseproof-api.railway.app` |
| `NEXT_PUBLIC_PROOF_REGISTRY_ADDRESS` | Адрес контракта ProofRegistry | `0x1234...` |
| `NEXT_PUBLIC_CHAIN_ID` | Chain ID сети Base | `84532` (Sepolia) или `8453` (Mainnet) |
| `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` | WalletConnect Project ID | Получить на cloud.walletconnect.com |

### Как получить WalletConnect Project ID:

1. Зайдите на https://cloud.walletconnect.com
2. Создайте новый проект (или используйте существующий)
3. Скопируйте **Project ID**
4. Добавьте в переменные окружения Vercel

## ✅ Проверка деплоя

После деплоя проверьте:

1. **Откройте URL деплоя** (например, `https://baseproof-xxx.vercel.app`)
2. **Проверьте консоль браузера** (F12) на наличие ошибок
3. **Попробуйте подключить кошелек**
4. **Проверьте загрузку данных** (proofs для адреса)

### Проверка переменных окружения:

В консоли браузера выполните:

```javascript
console.log({
  apiUrl: process.env.NEXT_PUBLIC_API_URL,
  chainId: process.env.NEXT_PUBLIC_CHAIN_ID,
  contract: process.env.NEXT_PUBLIC_PROOF_REGISTRY_ADDRESS
});
```

## 🔄 Передеплой после изменений

### Автоматический передеплой:

Vercel автоматически передеплоит при каждом push в `main`:

```bash
git add .
git commit -m "Update frontend"
git push origin main
```

### Ручной передеплой:

1. Зайдите в Vercel Dashboard
2. Выберите проект
3. Вкладка **"Deployments"**
4. Нажмите **"Redeploy"** на последнем деплое

### Передеплой через CLI:

```bash
cd frontend
vercel --prod
```

## 🔄 Обновление переменных окружения

Если вы обновили переменные окружения в Vercel:

1. **Обновите переменные** в Vercel Dashboard → Settings → Environment Variables
2. **Обязательно передеплойте проект!** Переменные применяются только при новом деплое

Быстрый способ:

```bash
# Сделайте пустой коммит для триггера передеплоя
git commit --allow-empty -m "chore: trigger redeploy after env update"
git push origin main
```

## 🐛 Решение проблем

### Проблема: Build fails

**Решение:**
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что проект собирается локально: `cd frontend && npm run build`
3. Проверьте версии Node.js (Vercel использует Node.js 18+)

### Проблема: Переменные окружения не применяются

**Решение:**
1. Убедитесь, что переменные начинаются с `NEXT_PUBLIC_`
2. Проверьте, что переменные добавлены для правильного окружения (Production)
3. **Передеплойте проект** после обновления переменных

### Проблема: WalletConnect не работает

**Решение:**
1. Проверьте, что `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID` установлен
2. Убедитесь, что Project ID правильный
3. Проверьте домен в настройках WalletConnect проекта

### Проблема: API запросы не работают

**Решение:**
1. Проверьте `NEXT_PUBLIC_API_URL` в переменных окружения
2. Убедитесь, что API доступен и CORS настроен
3. Проверьте Network tab в консоли браузера

## 📝 Настройка кастомного домена

1. В Vercel Dashboard → Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

## 🔗 Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [WalletConnect Cloud](https://cloud.walletconnect.com)

---

**После успешного деплоя обновите README.md с ссылкой на деплой!** 🚀

