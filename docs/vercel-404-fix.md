# 🔧 Исправление ошибки 404 на Vercel

Если после деплоя вы видите ошибку `404: NOT_FOUND`, выполните следующие шаги:

## ✅ Решение

### Шаг 1: Проверьте настройки проекта в Vercel Dashboard

1. Зайдите на https://vercel.com
2. Откройте ваш проект `baseproof`
3. Перейдите в **Settings** → **General**
4. Найдите раздел **Root Directory**
5. Установите: `frontend`
6. Нажмите **Save**

### Шаг 2: Передеплойте проект

После изменения Root Directory:

1. Перейдите во вкладку **Deployments**
2. Нажмите на три точки (⋮) на последнем деплое
3. Выберите **Redeploy**
4. Или сделайте пустой коммит:

```bash
git commit --allow-empty -m "chore: trigger redeploy after root directory fix"
git push origin main
```

### Шаг 3: Проверьте переменные окружения

Убедитесь, что в Vercel установлены все необходимые переменные:

1. **Settings** → **Environment Variables**
2. Проверьте наличие:
   - `NEXT_PUBLIC_API_URL`
   - `NEXT_PUBLIC_PROOF_REGISTRY_ADDRESS`
   - `NEXT_PUBLIC_CHAIN_ID`
   - `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID`

### Шаг 4: Проверьте структуру проекта

Vercel должен видеть следующую структуру:

```
baseproof/
└── frontend/
    ├── package.json
    ├── next.config.js
    ├── src/
    └── ...
```

## 🔍 Альтернативное решение

Если проблема сохраняется, попробуйте:

1. **Удалите проект из Vercel** (Settings → Delete Project)
2. **Создайте проект заново**
3. При импорте **сразу установите Root Directory: `frontend`**
4. Добавьте переменные окружения
5. Задеплойте

## 📝 Проверка

После исправления:

1. Откройте URL деплоя
2. Должна загрузиться главная страница BaseProof
3. Проверьте консоль браузера (F12) на наличие ошибок

---

**Если проблема не решена**, проверьте логи деплоя в Vercel Dashboard → Deployments → [ваш деплой] → Build Logs

